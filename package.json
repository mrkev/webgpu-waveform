{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run --workspace=webgpu-waveform dev",
    "build": "npm run --workspace=webgpu-waveform build",
    "build:site": "npm run --workspace=webgpu-waveform build:site",
    "build:lib": "npm run --workspace=webgpu-waveform build:lib",
    "lint": "npm run --workspace=webgpu-waveform lint",
    "preview": "npm run --workspace=webgpu-waveform preview",
    "test": "npm run --workspace=webgpu-waveform test'"
  },
  "devDependencies": {
    "lerna": "^8.1.5"
  }
}
